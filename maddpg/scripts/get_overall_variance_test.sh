if [ "$2" == "avg_var" ]; then
	echo "=================================================================="
	echo $1
	echo "=================================================================="
	./get_variance_test.sh $1 > x
	awk 'BEGIN{avg=0.0; count=0} { if($1!="For"){avg+=$0; count+=1}} END{print avg/count}' x
	rm x
	echo "      "
else
	echo "=================================================================="
        echo $1
        echo "=================================================================="
	./get_variance_test.sh $1
	echo "      "
fi
